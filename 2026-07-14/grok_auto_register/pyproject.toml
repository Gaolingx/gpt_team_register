[project]
name = "grok-auto-register"
version = "2.0.0"
description = "Grok 自动注册机（分层架构：mail / session / credential / orchestrator）"
requires-python = ">=3.13,<3.14"
dependencies = [
    "DrissionPage>=4.1",
    "curl_cffi>=0.7",
    "certifi>=2024.0.0",
]

[tool.uv]
package = false
